{{- if .Values.constraint-template.enabled }}
metadata:
  name: psp-privileged-container
  labels:
    # Tus etiquetas aquí

spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Pod"]
{{- end }}
